import { CharCheckPipePipe } from './char-check-pipe.pipe';

describe('CharCheckPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CharCheckPipePipe();
    expect(pipe).toBeTruthy();
  });
});
